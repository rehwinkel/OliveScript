basic_arithmetic = fun(a, b) {
    print(-(b+a)); # negate
    print(a + b); # add
    print(a - b); # subtract
    print(a * b); # multiply
    print(a % b); # modulo
    print(a / b); # regular (float) division
    print(a // b); # flooring division
};

bit_arithmetic = fun(a, b) {
    print(a ^ b); # xor
    print(a | b); # or
    print(a & b); # and
    print(a << b); # left shift
    print(a >> b); # right shift
    print(a >>> b); # unsigned right shift
};

# integers
int = 2462;
one_to_nine = 123456789;
# floats
float = 23.751;
pi = 3.1415;

# bitwise arithmetic
bit_arithmetic(int, float);
bit_arithmetic(int, one_to_nine);
bit_arithmetic(int, pi);
bit_arithmetic(float, one_to_nine);
bit_arithmetic(float, pi);
bit_arithmetic(one_to_nine, pi);

# "regular" arithmetic
basic_arithmetic(int, float);
basic_arithmetic(int, one_to_nine);
basic_arithmetic(int, pi);
basic_arithmetic(float, one_to_nine);
basic_arithmetic(float, pi);
basic_arithmetic(one_to_nine, pi);